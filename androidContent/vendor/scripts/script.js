function changeType(newType) {
	var el = $('#gallery_page #type');
	// var elType = 'dog';
	// 
	// if (el.hasClass('cat')) { elType = 'cat'; }
	// else if (el.hasClass('bird')) { elType = 'bird'; }
	// else if (el.hasClass('rabbit')) { elType = 'rabbit'; }
	// else if (el.hasClass('other')) { elType = 'other'; }
	// 
	// el.removeClass(elType);
	el.addClass(newType);
}